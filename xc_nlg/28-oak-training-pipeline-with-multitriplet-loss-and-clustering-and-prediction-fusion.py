# AUTOGENERATED! DO NOT EDIT! File to edit: ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb.

# %% auto 0
__all__ = ['block', 'args', 'mname', 'bsz', 'model', 'trie', 'train_dset', 'metric', 'learn', 'pred_dir', 'test_dset']

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 4
import os,torch, torch.multiprocessing as mp, pickle
from xcai.basics import *
from xcai.models.MMM0XX import DBT013

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 7
os.environ['CUDA_VISIBLE_DEVICES'] = '6,7'
os.environ['WANDB_PROJECT']='xc-nlg_22-oak-training-pipeline-with-multitriplet-loss-and-clustering'

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 9
block = XCBlock.from_cfg('/home/aiscuser/scratch/datasets', 'data', valid_pct=0.001, tfm='xcnlg', 
                         tokenizer='distilbert-base-uncased', smp_features=[('lbl2data',1,2)])

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 10
args = XCLearningArguments(
    output_dir='/home/aiscuser/scratch/Projects/xc_nlg/outputs/22-oak-training-pipeline-with-multitriplet-loss-and-clustering-2-14',
    logging_first_step=True,
    per_device_train_batch_size=800,
    per_device_eval_batch_size=800,
    representation_num_beams=200,
    representation_accumulation_steps=100,
    save_strategy="steps",
    evaluation_strategy='steps',
    eval_steps=1000,
    save_steps=1000,
    save_total_limit=5,
    num_train_epochs=50,
    predict_with_representation=True,
    adam_epsilon=1e-6,
    warmup_steps=100,
    weight_decay=0.1,
    learning_rate=2e-4,
    generation_num_beams=10,
    generation_length_penalty=1.5,
    predict_with_generation=True,
    representation_search_type='BRUTEFORCE',
    group_by_cluster=True,
    num_clustering_warmup_epochs=2,
    num_cluster_update_epochs=2,
    num_cluster_size_update_epochs=4,
    clustering_type='EXPO',
    minimum_cluster_size=1,
    maximum_cluster_size=300,
    output_concatenation_weight=1.0,
    target_indices_key='plbl2data_idx',
    target_pointer_key='plbl2data_data2ptr',
)

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 13
mname = f'{args.output_dir}/{os.path.basename(get_best_model(args.output_dir))}'
bsz = max(args.per_device_train_batch_size, args.per_device_eval_batch_size)*2

model = DBT013.from_pretrained(mname, ig_tok=0, bsz=bsz, tn_targ=1000, margin=0.3, tau=0.1, n_negatives=5, 
                               apply_softmax=True, lw=0.01, tie_word_embeddings=False)

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 15
trie = XCTrie.from_block(block)

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 18
train_dset = block.train.dset.sample(n=50_000, seed=50)
metric = PrecRecl(block.n_lbl, train_dset.data.data_lbl_filterer, prop=block.train.dset.data.data_lbl,
                  pk=10, rk=200, rep_pk=[1, 3, 5, 10], rep_rk=[10, 100, 200])

learn = XCLearner(
    model=model, 
    args=args,
    trie=trie,
    train_dataset=block.train.dset,
    eval_dataset=train_dset,
    data_collator=block.collator,
    compute_metrics=metric,
)

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 19
if __name__ == '__main__':
    mp.freeze_support()
    train_pred = learn.predict(train_dset)
    
display_metric(train_pred.metrics)

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 20
pred_dir = f'{mname}/predictions/'
os.makedirs(pred_dir, exist_ok=True)
with open(f'{pred_dir}/train_predictions.pkl', 'wb') as file: pickle.dump(train_pred, file)

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 22
test_dset = block.test.dset.sample(n=2000, seed=50)
metric = PrecRecl(block.n_lbl, block.test.data_lbl_filterer, prop=block.train.dset.data.data_lbl,
                  pk=10, rk=200, rep_pk=[1, 3, 5, 10], rep_rk=[10, 100, 200])

learn = XCLearner(
    model=model, 
    args=args,
    trie=trie,
    train_dataset=block.train.dset,
    eval_dataset=test_dset,
    data_collator=block.collator,
    compute_metrics=metric,
)

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 23
if __name__ == '__main__':
    mp.freeze_support()
    test_pred = learn.predict(block.test.dset)
    
display_metric(test_pred.metrics)

# %% ../nbs/28-oak-training-pipeline-with-multitriplet-loss-and-clustering-and-prediction-fusion.ipynb 24
pred_dir = f'{mname}/predictions/'
os.makedirs(pred_dir, exist_ok=True)
with open(f'{pred_dir}/test_predictions.pkl', 'wb') as file: pickle.dump(test_pred, file)
